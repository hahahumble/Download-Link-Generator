// Default config if config not exist in storage
export default {
  setting: {
    dropbox_convert: true,
    gdrive_convert: true,
    onedrive_convert: true,
    auto_paste: true,
    extract_link: false
  }
}
